[build]
  command = "NODE_OPTIONS='--openssl-legacy-provider' npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

[context.production.environment]
  NODE_ENV = "production"
